---
title: Mitigating Hallucinations in Large Language Models Via Decoder Layer Skipping
title_zh: 通过解码器层跳跃缓解大型语言模型中的幻觉
authors: "Hanze Li, Jinhao You, Yichen Guo, Kai Tang, Shuangyang Xie, Xiande Huang"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00819v1"
tags: ["query:ml-llm"]
score: 9.0
evidence: 提出DeLask，通过动态跳过解码器层缓解LLM幻觉
tldr: 大型语言模型生成文本时频繁出现幻觉，研究发现更深层解码器是主要来源。本文提出DeLask框架，利用Transformer与梯度下降的等价性，通过计算相邻解码步梯度的余弦相似度检测方向反转的层，动态跳过或部分聚合其隐藏状态。实验在多种模型和基准上证实，该方法显著降低幻觉，提升输出事实一致性。DeLask提供了一种轻量、通用的解码改进方案，增强了LLMs的可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00819v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1765, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00819v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 356, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00819v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1844, \"height\": 527, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00819v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 934, \"height\": 378, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00819v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 906, \"height\": 281, \"label\": \"Table\"}]"
motivation: 发现LLMs幻觉主要源于深层解码器，需针对性地抑制错误信号。
method: 利用Transformer与梯度下降等价性，定义driftance值检测梯度方向反转的层，动态跳过并部分融合隐藏状态。
result: 在多种LLM和基准上，DeLask显著降低幻觉频率，提升生成事实一致性。
conclusion: DeLask是一种轻量、通用的解码框架，有效提升LLMs鲁棒性和可靠性。
---

## 摘要
大型语言模型（LLMs）在多样化的自然语言任务中取得了强劲性能，但其输出常常出现幻觉——与事实信息不符的内容。在本工作中，我们对解码过程进行了全面的逐层分析，并揭示幻觉往往源自更深的解码器层。为解决这一问题，我们引入了DeLask（解码器层跳跃），一种新颖的解码框架，能够动态跳过易于产生幻觉的层。DeLask利用了这样的理论见解：一个L层Transformer的前向计算在条件上等价于L步梯度下降。我们通过计算连续解码步骤中梯度的余弦相似度来定义漂移值，从而在下降方向反转时识别出问题层。DeLask并非完全丢弃这些层，而是将其隐藏状态与前一层部分聚合，从而在抑制错误信号的同时保持一致性。在多种LLMs和基准上的广泛实验表明，DeLask能够持续缓解幻觉并增强整体可靠性，为提升大规模语言模型的鲁棒性提供了一种轻量级且可泛化的解码框架。

## Abstract
Large Language Models (LLMs) have achieved strong performance across diverse natural language tasks, yet their outputs often suffer from hallucinations -- content that is misaligned with factual information. In this work, we conduct a comprehensive layer-wise analysis of the decoding process and reveal that hallucinations tend to originate from deeper decoder layers. To address this issue, we introduce \textbf{DeLask} (\textbf{De}coder \textbf{La}yer \textbf{Sk}ipping), a novel decoding framework that dynamically skips layers prone to producing hallucinations. DeLask leverages the theoretical insight that the forward computation of an $L$-layer Transformer is conditionally equivalent to $L$ steps of gradient descent. We define a \emph{driftance value} by computing the cosine similarity between gradients derived from consecutive decoder steps, identifying problematic layers when the descent direction reverses. Rather than discarding such layers entirely, DeLask partially aggregates their hidden states with preceding layers, thereby preserving consistency while suppressing erroneous signals. Extensive experiments across diverse LLMs and benchmarks demonstrate that DeLask consistently mitigates hallucinations and enhances overall reliability, providing a lightweight and generalizable decoding framework for improving the robustness of large-scale language models.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLMs）在生成文本时频繁出现**幻觉**现象，即产生与事实不符或语义错误的内容，严重限制了其在医疗、法律等高风险领域的可靠性。
- **研究动机**：现有缓解幻觉的方法多依赖模型重训练（如微调、RLHF），成本高昂；部分推理时干预方法（如对比解码、logits修正）效果有限或不够轻量。作者希望提出一种**无需重训练、轻量级且可泛化**的解码框架。
- **关键发现**：通过对LLM解码器进行逐层分析，作者发现幻觉主要源自**深层解码器层**（尤其是中间层之后的层）。初步实验（图1b）显示，跳过某些深层后模型性能反而提升，验证了这一假设。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用Transformer前向计算与梯度下降的等价性理论，通过检测相邻解码步骤中梯度方向的反转（即从“下降”变为“上升”），识别出容易产生幻觉的层，并动态进行**部分层跳跃**，以抑制错误信号同时保持跨层一致性。
- **关键步骤与算法流程**：
  1. **早期退出**：对每个解码层 \( j \)，将隐藏状态 \( \mathbf{h}_j^t \) 通过最终线性层 \( \phi \) 和 softmax 得到概率分布 \( \mathbf{p}_j^t \)。
  2. **计算分布差**：相邻层的分布差 \( \Delta_j^t = \mathbf{p}_j^t - \mathbf{p}_{j-1}^t \)。
  3. **计算漂移值（Driftance Value）**：相邻分布差的余弦相似度 \( c_j^t = \frac{\Delta_j^t \cdot \Delta_{j-1}^t}{\|\Delta_j^t\| \|\Delta_{j-1}^t\|} \)。
  4. **判断幻觉层**：若 \( c_j^t < \tau \)（\( \tau \) 为负阈值），则标记该层为幻觉层。
  5. **部分跳跃**：对标记层，将当前层隐藏状态与前一层混合：\( \mathbf{h}_j^t \leftarrow \alpha \mathbf{h}_j^t + (1-\alpha) \mathbf{h}_{j-1}^t \)，其中 \( \alpha = \sigma(\beta c_j^t) \)（sigmoid映射），使漂移值越负时更多地依赖前一层状态。
- **应用范围**：仅对中间层 \( \lfloor N/2 \rfloor - 1 \) 到 \( N-2 \) 之间的层进行检测与调整（跳过最后一层，因为最终线性头严重依赖其输出）。

### 3. 实验设计

- **数据集/场景**：
  - 事实幻觉检测：TruthfulQA（MC1/MC2）、FACTOR（News/Wiki）
  - 多项选择推理：MMLU、ARC-Challenge
  - 开放生成：TriviaQA（阅读理解）、GSM8K（数学推理）、CoQA（对话问答）
- **基准（Baselines）**：共5种代表性解码策略——Regular（常规）、DOLA、ITI、UAD、Activation Decoding (AD)。
- **模型**：LLaMA2-7B、LLaMA3-8B。
- **评价框架**：lm-evaluation-harness，batch size固定为1。

### 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量及训练时长。仅提到推理时batch size为1，所有实验均基于公开评估框架。因此具体算力消耗信息缺失。

### 5. 实验数量与充分性

- **实验数量**：共在7个数据集上进行了主要对比实验（表1），并在LLaMA2-7B和LLaMA3-8B两个模型上重复，同时包含超参数消融（表2：τ和β的5种取值）和起始层消融（表3：从4/8/12/16/20/24层开始，5个数据集）。
- **充分性**：覆盖了三个典型任务类别（事实性、推理、开放生成），基线和模型规模具有代表性；消融实验探讨了关键超参数和起始位置的影响，验证了方法的稳定性。但未与最近的更多推理时方法（如CD、DoLa的变体）或更大模型（如LLaMA3-70B）对比，也未在长文本生成任务上测试。

### 6. 论文的主要结论与发现

- **主要结论**：DeLask通过动态跳过/部分融合深层幻觉层，能够在多种LLM和基准上**持续显著降低幻觉**，提升输出的事实一致性。在TruthfulQA、MMLU、GSM8K等任务上均优于所有基线（尤其LLaMA3-8B上表现突出），且仅需少量额外计算（只需计算中间层的概率分布差和余弦相似度）。
- **关键发现**：深层解码层是幻觉的主要来源；负漂移值与性能下降高度相关（图2）；起始于中间层（如第16层）的效果最好；超参数τ和β在合理范围内均能稳定发挥作用。

### 7. 优点

- **理论支撑**：将Transformer前向过程与梯度下降等价性联系起来，为层跳跃提供了理论依据。
- **轻量高效**：无需重训练，仅需对中间层进行极少量额外计算（分布差、余弦相似度），非常适合推理时集成。
- **动态自适应**：通过漂移值自适应地决定跳跃程度（α由sigmod映射），避免了硬跳过的破坏性。
- **实验广泛**：跨模型、跨任务、跨数据集验证，结果稳健。
- **有消融支持**：对阈值、缩放因子、起始层进行了系统分析，揭示了方法敏感度低、可配置性强。

### 8. 不足与局限

- **实验覆盖有限**：未在更大模型（如70B）或编码器-解码器架构（如T5）上验证；未测试长文本生成或对话场景下的幻觉缓解效果。
- **依赖中间层概率分布**：需要额外的前向计算（早期退出），虽然开销小，但在极低延迟场景下可能仍有影响。
- **阈值手动设定**：τ 和 β 需人工调优，不同模型或任务可能需要调整论文默认值（τ=-0.15, β=0.01）。
- **未分析协同效应**：未探讨与其他解码策略（如对比解码、采样温度调整）结合的可能性。
- **未披露资源细节**：缺少训练/推理的计算资源信息，难以复现性能与成本权衡。

（完）
