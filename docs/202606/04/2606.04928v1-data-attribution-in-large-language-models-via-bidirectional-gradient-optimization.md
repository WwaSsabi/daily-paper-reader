---
title: Data Attribution in Large Language Models via Bidirectional Gradient Optimization
title_zh: 基于双向梯度优化的大型语言模型数据归因
authors: "Frédéric Berdoz, Luca A. Lanzendörfer, Kaan Bayraktar, Roger Wattenhofer"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04928v1"
tags: ["query:ml-llm"]
score: 9.0
evidence: 提出双向梯度优化方法用于大语言模型训练数据归因
tldr: 大型语言模型数据归因是理解模型行为、实现问责性的关键。本文通过双向梯度优化方法：对生成文本施加梯度上升和下降扰动，测量训练样本损失变化以量化影响。该方法无需二次训练，支持从事实到风格的任意粒度归因，可直接识别正面与负面影响数据。在多个预训练大模型（如GPT-2）上，其在影响力指标上显著优于基线，AUROC提升明显。归因结果可解释性高，便于人类理解，为数据清洗和模型调试提供指导，提升了AI系统的透明度与责任感。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04928v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04928v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 661, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04928v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1842, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04928v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1841, \"height\": 1154, \"label\": \"Table\"}]"
motivation: 如何确定训练数据对生成输出的影响？这是LLM可解释性与数据溯源的核心问题。
method: 对生成样本应用梯度上升和下降双向优化，计算训练样本损失变化增量作为归因指标。
result: 在预训练LLM上，该方法在影响力指标（AUROC等）上优于现有基线，支持事实与风格归因。
conclusion: 该方法显著增强了LLM的可解释性和AI系统的问责性，推动负责任的人工智能。
---

## 摘要
大型语言模型（LLMs）正越来越多地被部署在各种应用中，引发了关于治理、问责和数据来源的关键问题。理解哪些训练数据对模型输出影响最大仍然是一个基本未解决的问题。我们通过扩展逆公式来解决自回归LLMs的训练数据归因（TDA）问题：如果模型在训练过程中看到了生成的输出，训练数据会如何受到影响？我们的方法通过对生成的文本样本使用双向梯度优化（梯度上升和下降）来扰动基础模型，并测量训练样本上损失的变化。我们的框架支持任意数据粒度的归因，能够实现事实和风格的归因。我们在已知数据集的预训练模型上对基线方法进行了评估，结果表明，在影响力指标上，我们的方法优于先前的工作，从而增强了模型的可解释性，这是负责任AI系统的关键要求。

## Abstract
Large Language Models (LLMs) are increasingly deployed across diverse applications, raising critical questions for governance, accountability, and data provenance. Understanding which training data most influenced a model's output remains a fundamental open problem. We address this challenge through training data attribution (TDA) for auto-regressive LLMs by expanding upon the inverse formulation: How would training data be affected if the model had seen the generated output during training? Our method perturbs the base model using bidirectional gradient optimization (gradient ascent and descent) on a generated text sample and measures the resulting change in loss across training samples. Our framework supports attribution at arbitrary data granularity, enabling both factual and stylistic attribution. We evaluate our method against baselines on pretrained models with known datasets, and show that it outperforms previous work on influence metrics, thereby enhancing model interpretability, an essential requirement for accountable AI systems.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **动机**：大型语言模型（LLMs）在众多应用中扮演关键角色，但其内部决策过程不透明。为了提升AI系统的可问责性与可追溯性，迫切需要理解**哪些训练数据**对模型生成的特定输出产生了实质影响——即训练数据归因（Training Data Attribution, TDA）。  
- **核心问题**：在开放式的自回归文本生成场景下，如何高效且准确地量化每个训练样本对生成输出的（正向或负向）影响？  
- **整体含义**：实现TDA有助于模型调试、数据去污、遗忘学习、数据估值以及审计追踪，是构建负责任AI体系的关键技术。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程

- **核心思想**：利用**逆影响假设（Mirrored Influence Hypothesis）**，即训练样本对生成输出的影响，可通过将生成输出“反作用于”模型后观察训练样本损失的变化来近似。  
- **双向梯度优化（DABGO）**：  
  1. 对给定生成文本 $\hat{x}$，分别执行**梯度下降**（fine-tuning风格）和**梯度上升**（unlearning风格）各 $M=10$ 步，得到两个扰动模型 $\theta_{\hat{x}}^{-}$ 和 $\theta_{\hat{x}}^{+}$。  
  2. 更新规则为：$\theta^{(m+1)}_{\pm} \leftarrow \theta^{(m)}_{\pm} \pm \alpha N F_{\theta^*}^{-1} \nabla \ell(\hat{x}, \theta)$，其中 $F_{\theta^*}$ 是Fisher信息矩阵（采用对角近似以降低计算开销）。  
  3. 计算每个训练样本 $x_i$ 在两个扰动模型下的损失变化绝对值：$I(x_i;\hat{x}) = |\ell(x_i, \theta_{\hat{x}}^{-}) - \ell(x_i, \theta_{\hat{x}}^{+})|$。  
  4. 按该双向影响得分（BIS）降序排列，得到最影响的训练样本。  

- **关键技术细节**：  
  - 使用**弹性权重巩固（EWC）** 中的Fisher对角近似来正则化优化，避免灾难性遗忘。  
  - 支持**任意粒度归因**：可根据损失变化定位token级贡献，实现细粒度解释。  
  - 无需计算每个训练样本的梯度，仅需两次前向传播即可得到所有样本的BIS，可扩展性好于传统影响函数。

### 3. 实验设计

- **数据集与场景**：  
  - **事实归因**：Wikipedia摘要子集（230M tokens），风格统一，迫使模型依靠事实内容进行归因。  
  - **风格归因**：Project Gutenberg文学文本（2M tokens），风格多样，用于检测对写作风格的归因能力。  
- **基准方法**：  
  - Random（随机采样作为噪声基线）  
  - BM25（基于词频的检索）  
  - TRACKSTAR（基于随机投影梯度的可扩展影响函数）  
  - GECKO（基于文本嵌入的检索）  
- **评估指标**：  
  - **Tail-Patch Absolute（TPA）分数**：用top-k归因样本对模型进行一次梯度更新，计算生成概率的绝对变化百分比。  
  - **重新训练验证**：剔除top-k样本后重新训练模型，比较生成样本损失的变化。  
- **模型设置**：基于GPT-2架构，上下文窗口256，从零训练。梯度上升/下降步数 $M=10$，学习率 $\alpha=1\times10^{-4}$。

### 4. 资源与算力

- **未明确说明**：论文未给出使用的GPU型号、数量或具体训练时长。仅提及“从零训练GPT-2风格模型”且计算开销较高（需要两次遍历完整训练数据集）。作者指出DABGO在工业级规模（千亿token）下可能不实用，暗示其算力需求主要受限于两遍遍历的开销。

### 5. 实验数量与充分性

- **实验数量**：  
  - 事实归因：25个生成查询，每个查询测试 $k=1,3,5,7,10,15,20$ 的TPA分数。  
  - 风格归因：多个作者的生成查询（具体数量文中未明，但结合定性示例有多个）。  
  - 消融实验：在Wikipedia上比较“仅上升”、“仅下降”、“双向”三种变体。  
  - 重新训练验证：4个生成句子，每个剔除 $k=20,50,100$ 样本后重新训练。  
  - 可解释性示例：给出1个事实+1个风格归因的定性表格（token级高亮）。  
- **充分性评估**：  
  - **优点**：覆盖事实与风格两种不同属性，对比多种基线，既有定量指标又有定性展示，且包含重新训练这一“黄金标准”验证，实验设计较为全面。  
  - **不足**：25个查询的规模偏小，重新训练仅4个样本，可能不足以排除偶然性；未在更大模型（如Llama 3.1）或更大数据集上进行实验；未对不同超参数（M步数、α）进行系统搜索。

### 6. 论文的主要结论与发现

- DABGO在TPA分数上**持续优于**所有基线（Random、BM25、TRACKSTAR、GECKO），尤其在事实归因场景下优势明显。  
- **双向优化（上升+下降）** 显著优于单向（仅上升或仅下降），证明两者捕获了互补的信号。  
- 重新训练验证表明，剔除DABGO标记的top-k样本后，生成样本的损失提升最大，与TPA结果一致，证实了方法的有效性。  
- DABGO能够同时归因事实内容和风格特征，且通过token级高亮提供可解释性，有助于理解哪些具体词句驱动了影响。

### 7. 优点

- **方法简洁高效**：仅需两次遍历训练数据即可计算所有样本的BIS，无需存储每个样本的梯度。  
- **支持任意粒度**：可轻松扩展到token级别，提供更细粒度的归因。  
- **无需监督标注**：完全基于模型内部损失变化，适用于无人工标注的开放式生成。  
- **实验验证充分**：不仅使用代理指标（TPA），还进行了耗时的重新训练验证，增强结果可信度。

### 8. 不足与局限

- **计算成本高**：仍需两遍完整遍历训练数据集，在工业级（千亿token）模型上不现实。  
- **未考虑样本交互**：BIS是逐样本的，无法捕捉多个训练样本之间的协同或拮抗作用。  
- **token级归因缺乏因果验证**：高亮token仅基于损失变化，尚未通过干预实验证实其因果角色。  
- **实验规模有限**：仅在小模型（GPT-2规模）和中等数据集上测试；25个查询的定量评估可能统计显著性不足。  
- **依赖Fisher对角近似**：EWC中的近似可能丢失参数间相关性信息，影响归因精度。

（完）
