---
title: Do Value Vectors in Deep Layers Need Context from the Residual Stream?
title_zh: 深层中的值向量是否需要来自残差流的上下文？
authors: "Muyu He, Yuchen Liu, Qingya Huang, Li Zhang"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02780v1"
tags: ["query:ml-llm"]
score: 9.0
evidence: Transformer深层值向量
tldr: 注意力层通常从残差流生成上下文相关的值向量。研究发现，深层注意力层仅使用无上下文的值向量即可有效保留原始Token信息，添加上下文依赖成分收益甚微。为此提出Bank of Values (BoV)，在最后三分之一的层中为每个Token学习独立的值向量查找表。在135M和780M模型上，BoV改善了验证损失，780M模型在21个基准上平均分与先前最佳方法持平，且计算和内存开销更低。该工作揭示了深层注意力无需上下文值向量的可能性，为设计高效注意力机制提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02780v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1527, \"height\": 988, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02780v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1634, \"height\": 505, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02780v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1344, \"height\": 860, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02780v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1649, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02780v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1639, \"height\": 602, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02780v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 814, \"height\": 632, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02780v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1572, \"height\": 440, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02780v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 812, \"height\": 174, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02780v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 815, \"height\": 1311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02780v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 814, \"height\": 1312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02780v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1263, \"height\": 1127, \"label\": \"Table\"}]"
motivation: 探究深层注意力层是否需要从残差流获取上下文来生成值向量，发现无上下文值向量即可有效保留原始Token信息。
method: 提出Bank of Values (BoV)，在最后三分之一注意力层中，通过查找表为每个Token学习独立的值向量，替代从残差流计算。
result: 在135M和780M参数模型上，BoV降低验证损失，780M模型在21个基准上平均分匹配先前最佳方法，且计算和内存更少。
conclusion: 深层注意力层可以仅使用无上下文的值向量，BoV方法简单高效，为改进Transformer注意力机制提供新思路。
---

## 摘要
Transformer架构作为现代大型语言模型骨干的成功在很大程度上归功于其注意力层的使用。注意力层遵循标准神经网络范式：它将残差流作为输入，从而生成依赖于上下文的查询、键和值向量。然而，我们发现当深层仅学习一个无上下文的值向量以保留原始标记信息，而不依赖于残差流中的任何上下文时，模型性能会显著提升。当模型能够访问这个无上下文的值向量时，再加回依赖上下文的组件对整体基准性能的提升微乎其微。这种无上下文的值向量可以作为稀疏模型参数存储，无需重新计算或持久缓存这些值。通过对这种无上下文值向量的关键设计选择进行系统消融，我们提出了价值库（Bank of Values, BoV），这是一种通过为最后三分之一层的每一层学习一个标记特定值向量的查找表来计算注意力中值向量的新方法。在135M和780M规模的模型上，BoV相比标准注意力降低了验证损失，并且在780M规模上，在21个基准测试中的平均得分与之前最佳方法相当，而该方法通过更少的计算和内存向值向量添加了标记信息。

## Abstract
The success of the transformer architecture as the backbone of modern LLMs is in large part due to its use of attention layers. An attention layer follows the standard neural network paradigm: it takes the residual stream as input and thereby produces context-dependent query, key, and value vectors. However, we find that model performance meaningfully improves when deeper layers learn only a context-free value vector to preserve the original token information, without drawing on any context from the residual stream. When the model has access to this context-free value vector, adding back the context-dependent component provides little additional benefit for aggregate benchmark performance. Such context-free value vectors can be stored as sparse model parameters, eliminating the need to recompute or persistently cache these values. Through systematic ablations on the key design choices for such context-free value vectors, we propose Bank of Values (BoV), a new way of computing value vectors in attention by learning a lookup table of token-specific value vectors for each of the last third of layers. Across 135M and 780M models, BoV improves validation loss over standard attention and, at 780M, the average score across 21 benchmarks, matching the previous best method that adds token information to the value vector with less compute and memory.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- 标准Transformer注意力层从残差流中计算上下文相关的查询、键和值向量，但值向量本身不参与跨token计算。
- 近期工作（如Value Residual Learning、MoVE等）发现向深层值向量注入原始token信息能提升性能，但尚未明确无上下文值向量单独使用的效果。
- 本文系统研究了深层值向量是否需要上下文，发现仅使用无上下文值向量（来自初始token嵌入）即可显著提升性能，上下文依赖组件贡献很小。

## 2. 论文提出的方法论
- **核心思想**：在最后三分之一的注意力层中，用查找表替代从残差流计算值向量，每个token id对应一个可学习的值向量。
- **关键技术细节**：
  - 定义值向量表 \( E_v \in \mathbb{R}^{|V| \times d} \)，其中 \( |V| \) 为词表大小，\( d \) 为隐藏维度。
  - 对于位置 \( p \)，值向量 \( v_p = \gamma_v \cdot E_v[i_p] \)，其中 \( \gamma_v \) 是每层可学习的缩放系数，\( i_p \) 是token id。
  - 推理时直接查表获取所有历史token的值向量，无需矩阵乘法或缓存，仅需O(Td)时间。
  - 内存节省公式：\( \Delta KV = \frac{L}{3} d (T - |V|) \)，当上下文长度 \( T > |V| \) 时，BoV占用更少内存。
- **初始化**：用 \( x_0 W_V \) 初始化每个目标层的表，使前向传播与原始值向量计算等价。

## 3. 实验设计
- **数据集**：训练使用ClimbMix预训练语料（子集），验证集约41.9M tokens；评估使用DCLM CORE的21个基准（Jeopardy, ARC-Easy/Challenge, COPA, HellaSwag, SQuAD, CoQA, BIG-bench等）。
- **Benchmark**：主要指标为验证损失（BPB）和21个基准的中心化准确率平均分（CORE score）。
- **对比方法**：
  - 标准注意力基线（baseline）
  - 早期无上下文值向量变体：直接使用 \( x_0 W_V \)、第一层值向量 \( V_1 \)，及其不同组合/系数设置
  - 先前方法：SVFormer（复制第一层值向量）、modded-nanogpt/MoVE（添加值embedding表）
  - 消融实验：共享表、固定系数、每层都应用、保留标准值向量V等。

## 4. 资源与算力
- **模型规模**：135M（12层）和780M（24层），隐藏维度分别为768和1536。
- **计算预算**：135M模型使用1.50×10^18 FLOPs（对应1.97B tokens）；780M模型使用3.91×10^19 FLOPs（对应8.19B tokens，BoV因FLOPs/token更低可多见token）。
- **优化器**：矩阵参数用Muon（lr=0.02或0.0283），其余参数（含值表）用AdamW。
- **硬件**：未明确GPU型号和数量，但提到使用4 GPU进行梯度累积。
- **训练时长**：未明确给出具体时间。

## 5. 实验数量与充分性
- **主实验**：两个模型规模（135M和780M），每个规模有多个变体（表1、表2）。
- **消融实验**：针对四个轴（替换 vs 添加、可学习系数、信息来源、目标层范围）共13种变体（表1）。
- **组件消融**：图5(b)包括共享表、固定系数、每层应用、保留V等。
- **对比实验**：与SVFormer、x0WV、V1、additive variant等比较（表2、表3、表5）。
- **充分性评估**：实验较系统，覆盖了设计选择和竞争方法。但作者承认仅验证到780M规模，更大模型的泛化性未知；此外未在纯长上下文任务上专门评估。

## 6. 论文的主要结论与发现
- 深层值向量主要受益于无上下文的原始token信息，上下文依赖组件贡献极小。
- BoV在验证损失上优于标准注意力基线（135M和780M），且在780M的21个基准平均分上匹配或超过先前最佳方法（如MoVE）。
- BoV在推理时无需计算或缓存值向量，节省FLOPs和内存（长上下文时优势明显）。
- 学习每层独立的查找表比共享表更好；仅应用于最后三分之一层比所有层好；可缩放系数带来增益。

## 7. 优点
- **方法简洁**：用O(1)查表替换密集矩阵乘法，降低计算和存储开销。
- **理论分析清晰**：给出内存节省公式，指出长上下文场景下的优势。
- **消融系统**：四个维度的消融实验揭示了深层偏好无上下文值向量的原因，具有可解释性。
- **公平对比**：在FLOPs控制下比较，确保计算资源等价。

## 8. 不足与局限
- **规模局限**：仅验证到780M参数，未在更大模型（如7B+）上实验，无法确认扩展性。
- **机制解释不足**：仅观察到深层偏好无上下文值向量，但未深入分析其优化目标或过平滑等机制。
- **内存权衡**：在短上下文（T<|V|）时，BoV占用更多内存，仅在长上下文时呈现优势。
- **任务覆盖**：主要评估通用NLP基准，未专门针对长文档或序列长度超过2K的任务。
- **计算资源**：未报告GPU型号和训练时间，可复现性略有不足。

（完）
