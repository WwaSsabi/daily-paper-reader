---
title: Improving Answer Extraction in Context-based Question Answering Systems Using LLMs
title_zh: 使用大语言模型改进基于上下文的问答系统中的答案提取
authors: "Hafez Abdelghaffar, Ahmed Alansary, Ali Hamdi"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06197v1"
tags: ["query:ml-llm"]
score: 9.0
evidence: 基于大语言模型的问答系统
tldr: "基于LLM的问答系统在从给定上下文中准确提取答案时仍面临挑战，尤其对复杂模糊查询易产生不精确响应。本研究提出在SQuAD1.1数据集上微调预训练LLM的方法，以强化其上下文理解与答案抽取能力。微调后的Roberta-base模型在ROUGE-L、BLEU和BERTScore上分别达到86.84%、28.24%和95.38%，表现最优。实验结果证实，针对性微调大幅提升了问答系统的可靠性与精确度。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06197v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 595, \"height\": 811, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06197v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1886, \"height\": 676, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06197v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 660, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06197v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 934, \"height\": 659, \"label\": \"Table\"}]"
motivation: 现有问答系统在处理复杂模糊查询时，难以从上下文中准确提取答案，常输出不相关或不精确的响应。
method: 基于预训练LLM，在SQuAD1.1数据集上微调Roberta-base模型，通过监督学习增强上下文理解和答案提取能力。
result: "微调后的Roberta-base在SQuAD1.1上取得ROUGE-L 86.84%、BLEU 28.24%、BERTScore 95.38%，性能最佳。"
conclusion: 定向微调有效提升了基于上下文的问答系统的准确性和可靠性。
---

## 摘要
随着大语言模型的出现，问答系统取得了显著进展。然而，在从给定上下文中准确提取和生成精确答案方面仍面临挑战，尤其是在处理复杂或模糊查询时。现有方法通常难以理解上下文、保持答案一致性以及跨领域泛化。本研究提出一种基于大语言模型的问答系统，输入为文本上下文和对应问题，输出为简洁准确的答案。该研究旨在解决当前问答系统的局限性，特别是即便获取了正确上下文也倾向于产生无关或不精确响应的问题。我们的方法包括在基准问答数据集上微调预训练的大语言模型，以提升其上下文理解和答案提取能力。具体地，我们使用了斯坦福问答数据集，该数据集提供了高质量的上下文-问题-答案三元组用于监督训练和评估。实验结果表明，微调后的Roberta-base模型表现最佳，在ROUGE-L指标上达到86.84%，BLEU指标为28.24%，BERTScore为95.38%。这些结果显示出高准确性和答案相关性，证明了该方法在基于上下文的问答任务中的有效性。此外，研究证实了有针对性的微调能显著提高问答系统的可靠性和精确度。

## Abstract
Question answering (QA) systems have achieved notable progress with the advent of large language models (LLMs). However, they still face challenges in accurately extracting and generating precise answers from given contexts, particularly when dealing with complex or ambiguous queries. Existing approaches often struggle with contextual understanding, answer consistency, and generalization across diverse domains. In this work, we propose a question answering system based on large language models, where the input consists of a textual context and a corresponding question, and the output is a concise and accurate answer. The motivation behind this research lies in addressing the limitations of current QA systems, particularly their tendency to produce irrelevant or imprecise responses despite having access to the correct context. Our methodology involves fine-tuning a pre-trained LLM on a benchmark QA dataset to improve its contextual comprehension and answer extraction capabilities. Specifically, we utilize the Stanford Question Answering Dataset (SQuAD1.1), which provides high-quality context-question-answer triplets for supervised training and evaluation. Experimental results show that the fine-tuned Roberta-base model achieves the highest performance, attaining a ROUGE-L score of 86.84%, a BLEU score of 28.24%, and a BERTScore of 95.38%. These results indicate strong accuracy and answer relevance, demonstrating the effectiveness of the proposed approach for context-based question answering tasks. Furthermore, the findings confirm that targeted fine-tuning substantially improves the reliability and precision of QA systems.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于上下文的问答系统（QA）在从给定文本中准确提取答案方面仍面临挑战，尤其是处理复杂或模糊查询时，常产生不精确或不相关的响应。此外，系统对噪声敏感、依赖参数知识而非上下文、存在“迷失在中间”的长上下文推理困难等。
- **整体含义**：尽管大语言模型（LLM）发展迅速，但仅靠预训练知识不足以完成高质量的上下文问答，需要针对任务进行微调以增强上下文理解和答案提取能力。本研究旨在通过系统化的微调方法提升QA系统的可靠性和精确度，具有实际应用价值。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：采用监督学习范式，对多个预训练Transformer模型进行微调，使其学会从给定上下文与问题中提取正确的答案片段（extractive QA）。输入为上下文和问题的拼接，输出为答案在输入中的起始和结束位置。
- **关键技术细节**：
  - 输入构建：\( x_i = [c_i; q_i] \)
  - 模型使用Transformer编码器生成上下文表示 \( H_i = \text{Transformer}_\theta(x_i) \)
  - 答案表示：预测起始索引 \( s_i \) 和结束索引 \( e_i \)，即 \( \hat{a}_i = (s_i, e_i) \)
  - 损失函数：交叉熵损失之和 \( \mathcal{L} = \mathcal{L}_{\text{start}} + \mathcal{L}_{\text{end}} \)，其中
    \[
    \mathcal{L}_{\text{start}} = -\sum_{i=1}^N \log P_{\text{start}}(s_i | x_i), \quad \mathcal{L}_{\text{end}} = -\sum_{i=1}^N \log P_{\text{end}}(e_i | x_i)
    \]
  - 优化目标：\( \theta^* = \arg\min_\theta \mathcal{L} \)
- **算法流程**（文字说明）：
  1. 加载预训练模型，初始化参数θ。
  2. 对每条训练样本，拼接上下文和问题，输入模型得到所有token的隐藏状态。
  3. 通过两个线性分类头分别预测每个token作为起始和结束的概率。
  4. 计算起始和结束位置的交叉熵损失并求和。
  5. 反向传播更新模型参数。
  6. 推理时，取概率最大的连续片段作为答案，确保答案源自上下文。

## 3. 实验设计：使用了哪些数据集/场景，benchmark是什么，对比了哪些方法

- **数据集**：Stanford Question Answering Dataset (SQuAD1.1)，包含超过10万个问答对，源自500多篇Wikipedia文章，每个答案均为上下文中的连续片段。划分为训练集和验证集。
- **Benchmark**：三个自动评估指标——ROUGE-L（最长公共子序列，衡量结构相似性）、BLEU（n-gram精确度）、BERTScore（基于上下文嵌入的语义相似度）。
- **对比方法**：共12个模型，包括不同大小和架构的预训练模型：
  - 小模型：Bert-tiny (4.4M)、Electra-small-discriminator (14M)、Albert-base-v2 (11.8M)、Distilbert-base-uncased (66M)、Bert-base-uncased (110M)、Roberta-base (125M)
  - 中等模型：SmolLM2 (135M)、Phi (1.3B)、Qwen2.5 (1.5B)、Stablelm-2 (1.6B)、Bloom (1.7B)
  - 大模型：TinyLlama-Chat (1.1B)
- **实验设置**：所有模型在相同预处理步骤和超参数下独立微调，并在验证集上评估。

## 4. 资源与算力

- **文中未明确说明**：论文未提及所使用的GPU型号、数量、训练时长、内存消耗等算力信息。仅提到模型参数量（从4.4M到1.7B），但未给出具体硬件配置或训练成本。

## 5. 实验数量与充分性

- **实验数量**：
  - 基线实验：在12个模型上进行了未微调的性能测试（表II）。
  - 微调实验：同一批12个模型在SQuAD1.1上微调后评估（表III）。
  - 总计：24组实验结果（12个模型 × 2种状态），覆盖了从极小型到中等规模的多种架构。
- **充分性评价**：
  - **优点**：对比模型多样，包括BERT族、RoBERTa、ALBERT、TinyLlama、Bloom、Phi、Qwen2.5等，具有代表性；使用三个互补指标（词汇重叠、n-gram精确度、语义相似度）全面评估。
  - **不足**：
    - 未进行消融实验（如不同学习率、批大小、训练步数影响）。
    - 未评估在SQuAD2.0（含不可回答问题）上的表现，缺少对“判断无答案”能力的检验。
    - 未对比检索增强生成（RAG）或多模型融合等更先进方法。
    - 仅在单一数据集上实验，缺乏跨领域泛化验证（如生物医学、法律等专业领域）。
  - 总体而言，实验基线对比充分，但深度和广度有限。

## 6. 论文的主要结论与发现

- 微调显著提升所有模型的性能：ROUGE-L、BLEU、BERTScore均大幅增长。
- **最佳模型**：Roberta-base（125M参数）微调后达到最高分：ROUGE-L 86.84%、BLEU 28.24%、BERTScore 95.38%。
- **架构对比**：微调后，编码器类模型（Roberta、Albert、Bert）的表现优于解码器类模型（如Stablelm-2、Qwen2.5），表明适用于extractive QA。
- **模型容量影响**：极小模型（Bert-tiny）提升有限，容量较大模型受益更多。
- 预训练知识不足以应对问答任务，任务特定微调至关重要。

## 7. 优点

- **方法论清晰**：采用标准的监督微调框架，简洁易懂，可复现。
- **对比全面**：覆盖了从轻量到中量的多种代表性预训练模型，提供了公平比较基准。
- **评估指标多样**：同时使用ROUGE-L（词汇结构）、BLEU（n-gram精确度）和BERTScore（语义相似度），避免单一指标的偏差。
- **实际意义**：验证了简单微调即可获得高性能，无需复杂检索或知识增强，对低资源场景具有参考价值。
- **开源数据集**：使用广泛认可的SQuAD1.1，结果易于与其他研究对比。

## 8. 不足与局限

- **数据集单一**：仅使用SQuAD1.1，未在SQuAD2.0、自然问题（Natural Questions）或其他领域数据集上验证，泛化性存疑。
- **缺乏消融实验**：未分析不同超参数、优化策略或数据量对结果的影响。
- **未处理不可回答问题**：SQuAD1.1不包含无答案问题，无法评估模型拒绝回答的能力。
- **无检索或外部知识集成**：论文仅依赖给定上下文，未尝试RAG或知识图谱增强，这些方法在复杂场景中可能更有效。
- **算力信息缺失**：未报告训练时间、GPU型号等，不利于复现和能耗评估。
- **指标局限性**：ROUGE-L和BLEU对同义词和句法变化不敏感，BERTScore虽好但可能受嵌入模型影响。
- **未测试最新大型模型**：实验模型最大约1.7B，未包括7B/13B等更大LLaMA、GPT系列，结论可能不适用于更大模型。
- **无错误分析**：未对失败案例进行定性分析，如模型在复杂推理或长上下文上的具体表现。

（完）
